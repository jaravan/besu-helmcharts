apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-node-permissions
  labels:
    {{- include "besu-sandbox.labels" . | nindent 4 }}
data:
  nodes-allowlist.yml: |
{{ include "besu-sandbox.nodesAllowlist" . | indent 4 }}
